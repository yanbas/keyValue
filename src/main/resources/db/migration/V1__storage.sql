CREATE TABLE storage (
                                  id varchar(255) NULL,
                                  value text NULL,
                                  CONSTRAINT storage_pk PRIMARY KEY (id)
);
